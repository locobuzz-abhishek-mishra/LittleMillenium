import { PaginatorNumberDirective } from './paginator-number.directive';

describe('PaginatorNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginatorNumberDirective();
    expect(directive).toBeTruthy();
  });
});
